'use strict';
import { findNodeAtLocation } from 'jsonc-parser';
export function getComponentGroups(document) {
    var node = findNodeAtLocation(document.root, ["minecraft:entity", "component_groups"]);
    if (node && node.type === "object") {
        return node.properties.map(function (p) { return p.keyNode.value; });
    }
    return [];
}
//# sourceMappingURL=serverEntity.js.map