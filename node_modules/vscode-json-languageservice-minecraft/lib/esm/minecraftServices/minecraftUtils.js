'use strict';
import * as Strings from '../utils/strings';
export function getMinecraftDocumentType(document) {
    if (document && document.root && document.root.type === 'object') {
        var properties = document.root.properties.filter(function (p) { return Strings.startsWith(p.keyNode.value, "minecraft:") && p.valueNode && p.valueNode.type === "object"; });
        if (properties.length === 1) {
            var type = properties[0].keyNode.value.substring("minecraft:".length);
            return type;
        }
    }
    return null;
}
export var MinecraftDocumentType;
(function (MinecraftDocumentType) {
    MinecraftDocumentType["ServerEntity"] = "entity";
})(MinecraftDocumentType || (MinecraftDocumentType = {}));
//# sourceMappingURL=minecraftUtils.js.map